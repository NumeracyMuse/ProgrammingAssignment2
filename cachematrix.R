## Function to calculate the inverse of a matrix 
## and then display the function

##   read in the matrix
makeCacheMatrix <- function(x = matrix()) {
        
	##   hold the matrix
        getnet <- function() x
        holdnet <- function(write, getnet) 

	##   display the the matrix
        list(holdnet)

}

 
## solve (inverse) the matrix
cacheSolve <- function(x, ...) {
        ## Return a matrix that is the inverse of 'x'
	    
	##   invert the matrix
        solvenet <- function(inverse, holdnet) 

	##   display the the matrix
        list(solvenet)
	
	
	
}
